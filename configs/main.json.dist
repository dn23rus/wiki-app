{
  "server": {
    "listen": "${APP_LISTEN_ADDRESS}"
  },
  "database": {
    "driver" : "mysql",
    "dsn": "${DB_USER}:${DB_PASSWORD}@tcp(${DB_HOST}:3306)/${DB_NAME}",
    "dbname": "${DB_NAME}"
  },
  "template_dir": "web/templates"
}
